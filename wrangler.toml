name                   = "carat-cloud"
pages_build_output_dir = "public"
compatibility_date     = "2024-09-23"

# ── D1 Database ────────────────────────────────────────────────────────────
# D1 binding is configured via Cloudflare dashboard:
# Workers & Pages > carat-cloud > Settings > Bindings > + Add > D1 Database
# Variable name: DB, D1 database: carat-cloud-db
